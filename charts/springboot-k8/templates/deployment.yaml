apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "springboot-k8.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "springboot-k8.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "springboot-k8.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
